name: Deploy Infrastructure to Dev

on:
  push:
    branches:
      - feature/dev
    paths:
      - 'infra/**'

permissions:
  id-token: write
  contents: read

jobs:
  deploy-dev:
    name: Deploy Infrastructure to Dev Environment
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
        
    #   - name: Deploy Bicep to Dev
    #     uses: azure/arm-deploy@v1
    #     with:
    #       subscriptionId: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
    #       resourceGroupName: ${{ secrets.AZURE_RESOURCE_GROUP_DEV }}
    #       template: infra/main.bicep
    #       parameters: '@infra/environments/main.dev.bicepparam'
    #       deploymentMode: Incremental
    #       scope: resourcegroup

    #   - name: Deployment
    #     uses: azure/bicep-deploy@v2
    #     with:
    #       type: deployment
    #       operation: create
    #       name: dev
    #       location: australiaeast
    #       scope: resourcegroup
    #       subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
    #       template-file: ./infra/main.bicep
    #       parameters: './infra/environments/main.dev.json'
    #       resource-group-name: ${{ secrets.AZURE_RESOURCE_GROUP_DEV }}

      - name: Deploy Resources
        run: |
          az deployment group create \
            --name 'dev' \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP_DEV }} \
            --template-file ./infra/main.bicep \
            --parameters ./infra/environments/main.dev.bicepparam
        id: deploy